# Configuration file for RefReadTest_t

process TEST = {

	path p = {Analysis}

	module Analysis = OtherThingAnalyzer {untracked int32 debugLevel = 1}

	source = PoolRASource {
		untracked vstring fileNames = {'a.root', 'b.root'}
		untracked int32 maxEvents = -1
	}

}
